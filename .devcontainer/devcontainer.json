{
    "name": "NAB-Node-HSM-Ubuntu",
    "build": {
        "dockerfile": "../Dockerfile"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "dbaeumer.vscode-eslint",
                "rvest.vs-code-prettier-eslint",
                "esbenp.prettier-vscode",
                "Orta.vscode-jest"
            ]
        }
    },
    "mounts": [
        "source=${localEnv:HOME}${localEnv:USERPROFILE}/.radicle,target=/home/node/.radicle,type=bind",
        "source=${localWorkspaceFolderBasename}-radicle,target=/home/node/.radicle/bin,type=volume",
        "source=${localWorkspaceFolderBasename}-node_modules,target=${containerWorkspaceFolder}/node_modules,type=volume",
        "source=${localWorkspaceFolderBasename}-dist,target=${containerWorkspaceFolder}/dist,type=volume"
    ],
    "postCreateCommand": "echo 'source <(npm completion)' >> /home/node/.bashrc && sudo chown -R node dist node_modules /home/node/.radicle/bin && softhsm2-util --free --init-token --label 'MyToken1' --so-pin '123456' --pin '12345' && curl -sSf https://radicle.xyz/install | sh && echo 'export PATH=$PATH:/home/node/.radicle/bin' >> /home/node/.bashrc"
}
